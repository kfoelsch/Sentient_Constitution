# Retired migration tools

Moved from `tools/` on **2026-06-17** during non-corpus housecleaning. These scripts supported one-off corpus structural rewrites (Chapter Five cluster migrations, Chapter Ten article renumbering, companion corpus splits, D/E/C widget attachment, CI/CF/CJS renumber passes, doc_architecture slim-down, and similar).

**Do not run** against the live corpus unless deliberately replaying history from git. Normal editing uses `make regression` and the maintenance scripts listed in [tools/README.md](../../tools/README.md).

Retrieve prior versions from git history if a script body is needed for reference.
